---
title: Инвентаризация сервера
updated: 2026-09-22
status: проверено read-only 2026-09-22
---

# Инвентаризация сервера

Первый шаг 1.1 [Плана реализации](План%20реализации.md). Проверка выполнена 2026-09-22 по SSH (хост `2vhutemas`) только чтением: запросы к PostgreSQL в режиме `default_transaction_read_only`, листинги каталогов, выборочные переменные окружения контейнеров без секретов. Изменений на сервере не вносилось.

## Сервисы

| Контейнер | Назначение |
|---|---|
| `supa_db` | PostgreSQL 17.6 (Supabase 17.6.1.054) — общая БД проекта |
| `supa_rest` | PostgREST v13.0.5; открытые схемы: `public, storage, v2` |
| `supa_auth` | GoTrue v2.185.0; регистрация закрыта (`GOTRUE_DISABLE_SIGNUP=true`) |
| `supa_kong`, `supa_storage` | Шлюз и Storage API Supabase |
| `app_media` | Медиасервис (Deno, `/opt/2vhutemas-services/media/index.ts`), данные в `/opt/2vhutemas-services/media-data` → `/data` (`orig`, `cache`, `tmp`), 2,4 ГБ |
| `app_timeline` | Deno 2.1.7, текущий таймлайн |
| `komplekt_*` | Отдельный проект со своим Supabase; к 2vhutemas не относится |

Каталоги: `/opt/2vhutemas` (compose, kong.yml, functions), `/opt/2vhutemas-services` (media, media-data, timeline, automaton), `/var/www/2vhutemas` (12 МБ), `/var/www/gis` (41 МБ). Диск: 79 ГБ, занято 42 %.

## Содержимое БД

Одна БД содержит несколько независимых контуров:

| Схема | Что это | Объём |
|---|---|---|
| `public` | Legacy-модель таймлайна | projects 10, authors 16, reference_entries 29, styles 14, media_assets 313 |
| `v2` | Уже созданная универсальная модель | entities 23 (все `period`), period_profile 23, targets 23; links, documents, attachments, object/person_profile, reference_items — пусто |
| `students` | Ведомость | student 41, work 42, submission 213, файлы работ; student_account, link_code, profile — пусто |
| `auth` | Аккаунты Supabase | users 3 |
| `isogd`, `pzz` | ГИС-контур (641 тыс. объектов) | Не входит в проект, но живёт в той же БД |

Схема `v2` соответствует исходной ER-диаграмме [Структуры БД](Структура%20БД.md): `bigint` ID, справочники по text code, триггеры создания `targets` и аудита. Колонки `links.role`, `documents.body_format`, `body_schema_version`, `search_vector` отсутствуют. Снимок схемы: `db/snapshots/20260922_v2_before.sql`.

Словари `v2`: entity_kinds (person, object, period); attachment_roles (cover, gallery, wiki, slogan, quote, source, justification); 11 object_types (от architecture_object до music); 6 reference_kinds; person_types (person, company, group).

Журнал миграций: `public.schema_migrations` пуст, `supabase_migrations` отсутствует. Фактическое состояние схем не зафиксировано миграциями.

### Медиа

`public.media_assets`: 313 записей, у всех заполнены `fs_path` и `sha256` (ключи вида `2026/06/<sha256>`). Классы: pdf 164, image 127, other 11, doc 8, video 3. `visibility=public` — 21, остальные private. `storage_path` заполнен только у 21 публичной записи. Колонки `bytes`, `width`, `height`, `mime_type`, `original_name` уже есть — их переносим в `media_files` как вариант original.

Происхождение части файлов — Telegram (`src_chat_id`, `src_message_id`).

### Ведомость и привязка

`students.student` уже содержит `tg_id`, `tg_handle`, `tg_status`, `phone`, `access_token`. Таблицы `student_account` и `link_code` существуют, но пусты: ни один студент не привязан к аккаунту. `students.staff` — 1 запись.

## Безопасность: найдено

**Критично — legacy `public`.** На таблицах `authors`, `styles`, `phenomena`, `phenomenon_style`, `reference_entries`, `reference_author`, `style_reference`, `project_feature`, `schema_migrations` RLS выключен, а роль `anon` имеет INSERT/UPDATE/DELETE/TRUNCATE. Схема `public` открыта через PostgREST. Значит, владелец публичного anon-ключа (он есть во фронтенде сайта) может изменять и удалять эти данные через REST. Проверка извне записью не выполнялась.

**`v2`.** RLS выключен. `anon` читает все таблицы, включая будущие черновики `documents`; любой `authenticated` может писать. Пока данные — только 23 периода.

**`students`.** Политики `auth_all` (`true`) дают любому вошедшему пользователю полный доступ ко всей ведомости: телефоны, `access_token`, оценки, работы. Схема не открыта в PostgREST, регистрация закрыта, аккаунтов 3 — поэтому сейчас риск ограничен. **Заводить студентов как `authenticated` при этих политиках нельзя** — это прямой блокер этапа 3.

**`public.media_assets`, `projects`, связи.** Политики `*_editor_all` дают любому `authenticated` полный доступ. Приемлемо, пока вошедшие — только администраторы.

Устранение — отдельная правка с согласия пользователя (см. [План реализации](План%20реализации.md), раздел «Что решить»).

## BackUp

2026-09-22 запущено первое снятие копии скриптом `tools/backup_server.py` в локальный `backups/<UTC>/`: `pg_dump -Fc` всей БД (133 МБ), `globals.sql`, tar каталогов проекта с media-data. Копия находится на рабочей станции, восстановление не проверено. Подробности — [BackUp](BackUp.md).

## Что осталось проверить

- Код `app_media` и `app_timeline`: какие таблицы и схемы читает сайт, есть ли запись.
- Какой ключ и какие схемы использует фронтенд `/var/www/2vhutemas`.
- `/opt/2vhutemas/functions` и `automaton`: кто пишет в `students` и `media_assets`.
- Наличие данных Editor.js в legacy-текстах.
