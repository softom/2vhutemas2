---
title: Схема данных — целевая
updated: 2026-09-23
status: целевая модель после Р-37, Р-38 и Р-39; все три этапа выполнены
---

# Схема данных

Целевое устройство схемы `app` после решений [Р-37 и Р-38](Решения.md): единая запись, дерево типов, предметные величины параметрами. Здесь описано то, к чему идём; что уже стоит в базе — в [Инвентаризации сервера](Инвентаризация%20сервера.md), история модели и обсуждения — в [Структуре БД](Структура%20БД.md).

Отличия от действующей базы помечены: **новое**, **изменяется**, **снимается**. Без пометки — работает сейчас и остаётся как есть.

## Обзор

```mermaid
flowchart TB
    subgraph ZAP["Записи"]
        ET["entity_types<br/>дерево типов"] --> E["entities<br/>запись"]
        E --- TG["tags<br/>метки"]
    end
    subgraph PAR["Предметные величины"]
        P["parameters<br/>параметр"] --> PS["parameter_sets<br/>набор"]
        PS -.привязан к узлу.-> ET
        E --> IND["indicators<br/>показатели"]
        IND --> IV["indicator_values<br/>значения"]
        P -.задаёт смысл.-> IV
    end
    subgraph SV["Связи и прикрепления"]
        E --> L["links<br/>связь с обоснованием"]
        E --> T["targets"]
        L --> T
        T --> AT["attachments<br/>одно содержимое"]
    end
    subgraph SOD["Содержимое"]
        AT --> D["documents"]
        AT --> MA["media_assets → media_files"]
        AT --> RI["reference_items"]
        AT --> PL["places"]
    end
    subgraph PUB["Авторство и публикация"]
        M["materials<br/>состояние"] --> R["revisions<br/>версии"]
        R --> RR["revision_reviews"]
        CO["contributors"] --> UR["user_roles → roles → permissions"]
    end
    E -.свой материал.-> M
    D -.свой материал.-> M
```

Правило, из которого следует остальное: **запись одна для всех**. Человек, здание и стиль отличаются типом и набором параметров, а не строением записи. Отображение, права, версии и публикация от типа не зависят.

## Записи и дерево типов

```mermaid
erDiagram
    entity_types {
        uuid id PK "Узел дерева"
        uuid parent_id FK "Родитель; пусто у корневой ветви"
        text code UK "Устойчивый код"
        text title_ru "Название"
        int sort_order "Порядок среди соседей"
    }
    entities {
        bigint id PK "Постоянный идентификатор записи"
        uuid type_id FK "Тип — узел дерева любой глубины, обязателен"
        text slug UK "Читаемый адрес, уникален целиком"
        text title_ru "Название"
        text title_original "Название на языке оригинала"
        text original_language "Язык оригинала"
        text title_la "Латинское наименование"
        text title_en "Английское название"
        bool is_published "Ведёт триггер от состояния материала"
        int sort_order "Ручной порядок показа"
        text color "Выделение в представлениях"
        timestamptz created_at "Аудит: создание, правка, авторы"
    }
    slug_history {
        text slug PK "Прежний адрес"
        bigint entity_id FK "Куда перенаправлять"
        timestamptz changed_at "Когда заменён"
    }
    tags {
        uuid id PK "Метка"
        text title "Написание, каким введено впервые"
    }
    entity_tags {
        bigint entity_id FK "Запись"
        uuid tag_id FK "Метка"
    }
    entity_types ||--o{ entity_types : parent_id
    entity_types ||--o{ entities : type_id
    entities ||--o{ slug_history : entity_id
    entities ||--o{ entity_tags : entity_id
    tags ||--o{ entity_tags : tag_id
```

| Что | Как |
|---|---|
| **новое** `entity_types` | Дерево. Корневые ветви — прежние виды: «Кто», «Что», «Когда». Ниже растут типы: здание, зрелищное здание, постановка, стиль |
| **изменяется** `entities.type_id` | Вместо `kind_id`. Тип обязателен и один |
| **снимается** `entity_kinds`, `object_types`, `person_types`, `period_types` | Сходятся в дерево |
| **снимается** `object_profile`, `person_profile`, `period_profile` | Типология и полное имя — параметры; координаты — места ([Р-25](Решения.md)); тип — узел дерева |
| **изменяется** `entities.slug` | Уникален целиком, а не в пределах вида: видов больше нет. `slug_history.kind_id` снимается |
| **снимается** `entities.cover_media_id` | Обложка — первое прикреплённое изображение по порядку ([Р-36](Решения.md)); второго места для того же сведения не держим |

Отбор идёт по ветви целиком: рекурсивный обход от выбранного узла вниз. Данных немного, материализованный путь пока не нужен; если список перестанет отвечать за 300 мс ([Р-17](Решения.md)), колонка пути добавляется отдельной миграцией.

## Предметные величины

```mermaid
erDiagram
    parameters {
        uuid id PK "Определение величины"
        text code UK "Устойчивый код"
        text title_ru "Название"
        text unit "Единица измерения, часть определения"
        text value_type "number, integer, text, boolean, option, date, place"
        text list_note "option — список; несколько ответов задаёт is_repeatable"
        bool is_repeatable "У вопроса бывает несколько ответов"
        text definition "Что именно считается этой величиной"
        int sort_order "Порядок в справочнике"
    }
    parameter_options {
        uuid id PK "Допустимое значение при value_type=option"
        uuid parameter_id FK "Чей список"
        text code "Код значения"
        text title_ru "Название значения"
    }
    parameter_sets {
        uuid id PK "Набор"
        text code UK "Код набора"
        text title_ru "Например «Зрелищное здание»"
    }
    parameter_set_items {
        uuid set_id FK "Набор"
        uuid parameter_id FK "Параметр в нём"
        int sort_order "Порядок заполнения"
        text hint "Подсказка для этого набора"
    }
    type_parameter_sets {
        uuid type_id FK "Узел дерева типов"
        uuid set_id FK "Набор действует и для всего, что ниже"
    }
    entity_parameter_sets {
        bigint entity_id FK "Исключение: набор у самой записи"
        uuid set_id FK "Например «Вокзал» у здания-музея"
    }
    indicators {
        uuid id PK "Показатели — одно измерение целиком"
        bigint entity_id FK "Чьи"
        text title "«По проекту», «после реконструкции»"
        bool is_current "По ним считаются сортировка и отбор"
        int measured_year "Когда получены сведения"
        text measured_by "Кем измерено или откуда взято"
        bigint source_reference_item_id FK "Источник"
        text note "Пояснение"
        int sort_order "Порядок показа"
    }
    indicator_values {
        uuid id PK "Значение"
        uuid indicator_id FK "В составе каких показателей"
        uuid parameter_id FK "Какая величина"
        numeric num_value "Число в базовой единице"
        text text_value "Текстовое значение"
        bool bool_value "Да или нет"
        uuid option_id FK "Выбор из списка параметра"
        uuid place_id FK "Место из справочника"
        int date_start_year "Дата: начало, год может быть отрицательным"
        int date_end_year "Дата: окончание сдвоенной"
        bool is_approximate "Приблизительно"
        bool is_ongoing "Открытый конец"
        text note "Пояснение к величине"
    }
    parameters ||--o{ parameter_options : parameter_id
    parameters ||--o{ parameter_set_items : parameter_id
    parameter_sets ||--o{ parameter_set_items : set_id
    parameter_sets ||--o{ type_parameter_sets : set_id
    parameter_sets ||--o{ entity_parameter_sets : set_id
    indicators ||--o{ indicator_values : indicator_id
    parameters ||--o{ indicator_values : parameter_id
```

Правила, которые держит база:

- У значения заполнено ровно то поле, которое соответствует типу параметра. Проверка по `value_type`, а не по договорённости.
- Один параметр встречается в одних показателях один раз: `UNIQUE (indicator_id, parameter_id)`.
- Единица измерения живёт в определении параметра; рядом со значением её нет.
- Набор подсказывает состав, но не запрещает заполнить параметр вне набора и не требует заполнить всё.
- Наследование по дереву: набор узла действует для всего, что ниже. Общее для «Что» не дублируется в «Здании».
- Сортировка и отбор считают по показателям с `is_current`; в карточке видны все.

**Датировки и места — параметры** ([Р-40](Решения.md), выполнено). Вид даты — рождение, строительство, открытие, реконструкция — это параметр с `value_type = date`; роль места — адрес объекта, место рождения, захоронение, офис — параметр с `value_type = place` и значением-ссылкой на справочник мест. Приблизительность и открытый конец живут у значения. `entity_dates`, `date_kinds` и `attachments.place_id` сняты, [Р-08](Архив%20решений.md) отменено.

Справочник мест для параметра «место» — то же, чем список значений служит параметру «выбор»: общий набор возможных ответов.

## Связи и прикрепления

```mermaid
erDiagram
    links {
        bigint id PK "Связь"
        bigint from_entity_id FK "Откуда"
        bigint to_entity_id FK "Куда"
        uuid role_id FK "Роль связи, необязательна"
        text note "Краткое пояснение"
        bool is_primary "Основная связь"
        text confidence "Степень уверенности"
    }
    targets {
        bigint id PK "Цель прикрепления"
        text kind "entity или link"
        bigint entity_id FK "Заполнено ровно одно"
        bigint link_id FK "из двух"
    }
    attachments {
        bigint id PK "Прикрепление"
        bigint target_id FK "К чему прикреплено"
        uuid role_id FK "Роль: иллюстрация, обоснование, адрес объекта"
        int sort_order "Порядок; первое изображение — обложка"
        bigint document_id FK "Ровно одно"
        uuid asset_id FK "из четырёх"
        bigint reference_item_id FK "видов"
        uuid place_id FK "содержимого"
        text note "Пояснение к прикреплению"
    }
    link_roles {
        uuid id PK "Словарь ролей связи"
        text code UK "Код"
        text title_ru "Название"
    }
    attachment_roles {
        uuid id PK "Словарь ролей прикрепления"
        text code UK "Код"
        text title_ru "Название"
    }
    links ||--|| targets : link_id
    targets ||--o{ attachments : target_id
    link_roles ||--o{ links : role_id
    attachment_roles ||--o{ attachments : role_id
```

Связь не существует без обоснования: отложенный триггер требует к моменту фиксации транзакции прикрепление с ролью `justification`. Роль принадлежит прикреплению, а не содержимому: одно и то же место служит адресом здания, местом рождения архитектора и офисом бюро.

## Содержимое: тексты, источники, медиа, места

```mermaid
erDiagram
    documents {
        bigint id PK "Документ"
        text title "Заголовок"
        text lang "Язык"
        text body_format "blocknote"
        int body_schema_version "Версия схемы блоков"
        jsonb body_json "Блоки редактора"
        text body_text "Производный текст для поиска"
        tsvector search_vector "Поисковый указатель"
    }
    document_entity_refs {
        uuid revision_id FK "Версия документа"
        text occurrence_id "Устойчивый идентификатор появления"
        bigint entity_id FK "Упомянутая запись"
        text display_mode "Карточка или упоминание в строке"
        uuid media_asset_id FK "Иллюстрация этого появления"
        int ordinal "Порядок в документе"
    }
    reference_items {
        bigint id PK "Источник, цитата, ссылка"
        uuid kind_id FK "Вид источника"
        text title "Заголовок"
        text text "Текст или цитата"
        text url "Адрес"
        int year "Год"
        int reliability "Надёжность"
    }
    media_assets {
        uuid id PK "Изображение в реестре"
        uuid kind_id FK "Вид: фотография, план, разрез"
        text caption_ru "Подпись"
        text credit "Автор снимка и атрибуция"
        text source_url "Страница происхождения"
        text license_code "Условия использования"
        bool is_published "Ведёт триггер от состояния материала"
        timestamptz archived_at "Архив без потери файлов"
    }
    media_files {
        uuid id PK "Физический вариант"
        uuid asset_id FK "Изображение"
        text variant "original, screen, thumbnail"
        uuid source_file_id FK "Оригинал для производной"
        text storage_key "Ключ в хранилище"
        text sha256 "Контрольная сумма"
        text status "pending, processing, ready, failed"
        bool is_current "Текущий вариант для выдачи"
    }
    places {
        uuid id PK "Место"
        text country "Страна"
        text settlement "Населённый пункт"
        text street "Улица"
        text house "Дом"
        text unit "Помещение"
        float lat "Широта, только парой с долготой"
        float lon "Долгота"
        text precision "Точка, здание, населённый пункт, область"
    }
    media_assets ||--|{ media_files : asset_id
    media_files o|--o{ media_files : source_file_id
    documents ||--o{ document_entity_refs : revision_id
```

Повтор адреса невозможен: частичный уникальный указатель по нормализованным элементам ([Р-35](Решения.md)). Метки файлов лежат в общем справочнике `tags` через `media_tags`.

## Авторство, версии и публикация

```mermaid
erDiagram
    materials {
        uuid id PK "Материал — единица публикации"
        text status "draft, published, archived"
        text kind "Что именно: запись, связь, документ, источник, файл"
        bigint entity_id FK "Ровно одна"
        bigint link_id FK "ссылка"
        bigint document_id FK "на"
        bigint reference_item_id FK "содержимое"
        uuid asset_id FK "материала"
        uuid published_revision_id FK "Что видит читатель"
        timestamptz archived_at "Когда убран из показа"
    }
    revisions {
        uuid id PK "Версия"
        uuid material_id FK "Чья"
        uuid base_revision_id FK "От какой версии правили"
        uuid edited_by FK "Кто правил"
        text operation "create, update, archive"
        text summary "Краткое описание правки"
        jsonb snapshot "Состояние целиком"
    }
    revision_reviews {
        uuid id PK "Решение по версии"
        uuid revision_id FK "О какой версии"
        uuid reviewer_id FK "Кто решил"
        text decision "submitted, approved, rejected, published"
        text note "Объяснение"
    }
    material_credits {
        uuid material_id FK "Материал"
        uuid contributor_id FK "Участник"
        text credit_role "Автор, редактор, переводчик"
    }
    contributors {
        uuid id PK "Участник"
        uuid auth_uid "Аккаунт входа"
        text display_name "Отображаемое имя"
        text status "Состояние учётной записи"
    }
    roles {
        uuid id PK "Роль"
        text code UK "su, editor, contributor, reader"
    }
    permissions {
        uuid id PK "Право"
        text action "view, edit, create_delete, publish, review, su"
    }
    role_permissions {
        uuid role_id FK "Роль"
        uuid permission_id FK "Право"
    }
    user_roles {
        uuid id PK "Выдача роли"
        uuid contributor_id FK "Кому"
        uuid role_id FK "Какая"
        timestamptz revoked_at "Когда отозвана"
    }
    materials ||--o{ revisions : material_id
    revisions ||--o{ revision_reviews : revision_id
    materials ||--o{ material_credits : material_id
    contributors ||--o{ material_credits : contributor_id
    contributors ||--o{ user_roles : contributor_id
    roles ||--o{ user_roles : role_id
    roles ||--o{ role_permissions : role_id
    permissions ||--o{ role_permissions : permission_id
```

**Показатели входят в материал записи** ([Р-38](Решения.md)): их правка создаёт версию записи и проходит обычную публикацию. Отдельного материала и отдельного авторства у измерений нет.

Одновременную правку разводит `base_revision_id`: версия поверх устаревшей отклоняется с `version_conflict`.

## Служебное

| Таблица | Назначение |
|---|---|
| `schema_migrations` | Журнал применённых миграций: номер, контрольная сумма, кто и когда применил ([Р-12](Решения.md)) |
| `import_jobs`, `import_items`, `import_key_map` | Пакетное наполнение с повторяемостью по внешнему ключу |
| `published_entity_mentions` | Представление: в каких опубликованных материалах упомянута запись. Черновики через него не раскрываются |

## Что меняется по этапам

| Этап | Появляется | Снимается |
|---|---|---|
| **А** — выполнен 2026-09-23 (0015) | `entity_types`, `entities.type_id` | `entity_kinds`, `object_types`, `person_types`, `period_types`, три профиля, `entities.cover_media_id`, `slug_history.kind_id` |
| **Б** — выполнен 2026-09-23 (0016) | `parameters`, `parameter_options`, `parameter_sets`, `parameter_set_items`, `type_parameter_sets`, `entity_parameter_sets`, `indicators`, `indicator_values` | `object_profile` |
| **В** — выполнен 2026-09-24 (0017) | Параметры с `value_type = date` и `value_type = place`, признак `is_repeatable` | `entity_dates`, `date_kinds`, `attachments.place_id` |

Переносить немного: одиннадцать записей с их типами, координаты уже в местах, датировок единицы. Через год та же работа стоила бы сотен записей — поэтому делается сейчас.

## Инварианты

- У записи ровно один тип; тип обязателен. Прежний составной ключ «профиль соответствует виду» больше не нужен.
- У `targets` заполнено ровно одно из `entity_id` и `link_id`; на запись и на связь приходится ровно одна цель.
- У `attachments` заполнено ровно одно содержимое из четырёх.
- Связь не существует без прикреплённого обоснования — отложенный триггер.
- У `materials` ровно одна ссылка на содержимое, и каждое содержимое принадлежит не более чем одному материалу.
- `base_revision_id` и `published_revision_id` принадлежат тому же материалу — составные внешние ключи.
- У значения заполнено поле, соответствующее типу параметра; параметр не повторяется в одних показателях.
- Публикация и доступ проверяются и для прикреплённого: черновик не раскрывается через вложения или API.

## Что требует подтверждения

- Состав первого набора «Зрелищное здание»: список величин и как каждая измеряется.
- Полный список типов значений — достаточно ли `number`, `integer`, `text`, `boolean`, `option`, `date`.
- Судьба колонок `media_assets`, убранных из формы ([Р-28](Решения.md)): описание, альтернативный текст, год съёмки, место хранения, инвентарный номер, подпись источника, `visibility`. Они пусты и в проверках доступа не участвуют, но снимать их без вашего слова не буду.
